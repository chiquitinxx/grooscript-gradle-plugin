buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath group: 'org.grooscript', name: 'grooscript-gradle-plugin', version: '0.1-SNAPSHOT'
    }
}

apply plugin: 'grooscript'
