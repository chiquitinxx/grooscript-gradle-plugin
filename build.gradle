apply plugin: 'idea'
apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'signing'

group = 'org.grooscript'
version = '0.4'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    testCompile "org.spockframework:spock-core:0.7-groovy-2.0", {
        exclude module: "groovy-all"
    }
    compile 'cglib:cglib-nodep:2.2'
    compile 'org.grooscript:grooscript:0.5.1', {
        exclude module: "groovy"
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = "2.0"
}

//Sonar repository stuff
/*task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from 'build/docs/javadoc'
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
    classifier = 'sources'
}

artifacts {
    archives jar
	archives javadocJar
    archives sourcesJar
}

signing {
    sign configurations.archives
}

uploadArchives {
    repositories {
        mavenDeployer {
            beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }

            repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
              authentication(userName: sonatypeUsername, password: sonatypePassword)
            }

            pom.project {
               name 'grooscript-gradle-plugin'
               packaging 'jar'
               description 'Grooscript gradle plugin'
               url 'http://grooscript.org/'

               scm {
                   url 'scm:git@github.com:chiquitinxx/grooscript-gradle-plugin.git'
                   connection 'scm:git@github.com:chiquitinxx/grooscript-gradle-plugin.git'
                   developerConnection 'scm:git@github.com:chiquitinxx/grooscript-gradle-plugin.git'
               }

               licenses {
                   license {
                       name 'The Apache Software License, Version 2.0'
                       url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                       distribution 'repo'
                   }
               }

               developers {
                   developer {
                       id 'chiquitinxx'
                       name 'Jorge Franco'
                   }
               }
           }
        }
    }
}*/
//End Sonar